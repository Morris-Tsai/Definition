p485

你可對Collection做什麼事
集合內加入物件
集合內移除物件
查詢集合內是否包含一個物件
從集合內取出一個物件
對集合進行迭代(iterate)操作，依序查看每一個元素


集合(collection)有三種用法
1.collection(小寫c)表示儲存物件與進行迭代動作的資料結構
2.Collection(大寫C)指的是java.util.Collection介面，也就是Set、List與Queue所擴充的
3.Collections(大寫C結尾為s)是指java.util.Collections這個類別，她有一堆static的工具函式來操作集合資料結構


Collections與Collection
1.Collections是一個類別，她有許多的public工具函式
2.Collection是一個介面，它定義了大部分集合資料結構所需要的函式，如add()、remove()、contains()與iterator()

集合(Collections)型別也四大類
1.List東西的表列、清單(實作List的類別)
2.Set內含唯一的東西(實作Set的類別)
3.Map內含唯一ID的東西(實作Map的類別)
4.Queue裡面的東西會依據它們要被處理而排列
